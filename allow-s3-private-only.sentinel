import "tfplan/v2" as tfplan
allow_private_only = rule {
	all filter tfplan.resource_changes as _, rc {
		rc.type is "aws_s3_bucket" and
			rc.mode is "managed" and
			rc.change.actions is ["create"]
	} as _, frc {
		frc.change.after.acl is  "private"
	}
}

main = rule {
	allow_private_only
}